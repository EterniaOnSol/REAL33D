# HANDOFF

Date/time: 2026-09-24 16:29 -06:00, America/Guatemala
Task: `REAL33D-AGENT-MVP-001`
Agent / role: Codex, implementation and live QA
Branch: `main`
Starting commit: `bd15cc0a49d8182dc1cc3732b8487859f1662044`
Ending commit: the commit containing this handoff
Worktrees: `C:\Users\dell\Desktop\fusion32` and separate
`C:\Users\dell\Desktop\REAL33D2D` (starting at
`aeef6aa939a764de3728c51a3022953dff97f375`)

## Objective and result

One autonomous 2D OTClient player entered authoritative Fusion32 under ordinary
player rights, observed only client-visible state, and used the normal `g_game`
path for chat, movement, attack/chase, container open/use, loot transfer and
food use. Named live checks and LuaJIT policy tests are `PASS`. Actual local
Ollama inference and sustained low-HP survival remain `IMPLEMENTED_UNVERIFIED`.
No REAL33D 3D/Unreal, ClientCore/WorldState, Fusion32 gameplay or protocol
extension was made for this task.

## Startup and authority inspected

- Read `AGENTS.md`, `PROJECT_STATUS.md`, `ARCHITECTURE.md`, `ROADMAP.md`,
  `PARITY_MATRIX.md`, and the prior `handoffs/CURRENT.md`; archived that handoff
  as `handoffs/archive/2026-09-23_UNREAL-COMBAT-FOLLOW-001.md`.
- `git status`, branch, HEAD and remote checked. Fusion32 had numerous
  pre-existing unrelated changes in Unreal/ClientCore/docs; they were
  preserved. REAL33D2D began clean. Origin is the existing repository remote;
  no push or remote operation was performed.
- Inspected REAL33D2D `src/client/luafunctions.cpp`, `game.cpp`, `mapview.cpp`,
  `container.h/cpp`, `item.cpp`, and existing UI container/move scripts.
  Inspected Fusion32 `reference/game/src/receiving.cc::{CAttack,CUseObject,CMoveObject}`,
  `crcombat.cc::{SetAttackDest,StopAttack}`, `moveuse.cc::UseFood`, and
  `sending.cc::SendClearTarget`. `Item::getName` is editor-only; shipped client
  item type names are empty, so the agent uses visible IDs and observed
  container changes.

## Discoveries and decisions

- Synthetic QA A initially had 111 GM rights, including `NO_ATTACK` and
  `INVULNERABLE`. B had no rights but died from poison during exploratory QA.
  At the operator's explicit request, Fusion32 was stopped, local QA SQLite
  was backed up to
  `/var/lib/fusion32-server-baseline-772-0/querymanager/state/tibia.before-agent-rights.sqlite`,
  and A's rights were removed; both A and B then read `NONE`. This setup change
  occurred outside the agent, with the server stopped. It changed no game rule
  or source and is not available to any Brain.
- A first mock policy followed too long, then cancelled a healthy deer every
  12 seconds. The accepted policy attacks immediately, turns on ordinary chase
  mode, and keeps attacking while the target stays visible. It never cancels a
  target chosen outside this brain.
- The client-visible `dead rabbit` contained ID 3577. One live run moved it
  into A's bag; the next login used that same visible carried ID. The bag count
  fell by one, the authoritative evidence of consumption.

## Changes and files

- `agent/real33d2d/modules/real33d_agent/{real33d_agent.otmod,agent_core.lua,agent_runtime.lua}`:
  opt-in gateway, bounded observation, intent validation, rolling budget,
  deterministic tactical Brain, local Ollama Brain, normal action dispatcher,
  fresh-state recheck, and session logging. The same module files were copied
  into separate REAL33D2D `modules/real33d_agent/` for the live test.
- `agent/real33d2d/{README.md,run_agent.sh,run_agent.ps1}`: usage and local QA
  launchers. PowerShell launcher was used live; credentials stayed process-local
  and were not printed or committed.
- `tests/real33d_agent_test.lua`: policy, viewport, budget, loot/eat and mocked
  provider tests.
- `evidence/agent/REAL33D-AGENT-MVP-001.md`: named checks, live trace and
  explicit limits. Raw logs remain ignored in `evidence/agent/live/`.
- `.gitignore`, `PROJECT_STATUS.md`, `PARITY_MATRIX.md`, this handoff and its
  archive were updated. Unrelated pre-existing changes remain untouched.

## Tests and evidence

- `C:\Users\dell\Desktop\REAL33D2D\build\vcpkg_installed\x64-windows\tools\luajit\luajit.exe tests/real33d_agent_test.lua`: PASS.
- `scripts/server/status_wsl.sh` before live QA: querymanager, game and login
  alive with verified executable, working directory, start identity and port.
  `scripts/server/stop_wsl.sh` after QA: all services stopped cleanly.
- Normal-rights A: login, Say echo, equipped bag open/use, server-confirmed
  walking. Deer and rabbit attacks produced visible HP reductions; final rabbit
  run had no agent `cancel_attack` before server clear-target. A maintained
  155/155 HP during a monitored multi-minute run.
- A opened a visible corpse, saw `dead rabbit[3577:x1]`, sent `move_item`
  `c:1:0 → c:0:2`, then observed empty corpse and bag containing 3577. In a
  later login, `use c:0:0` removed 3577 from the bag. Exact timestamps and
  preconditions are in `evidence/agent/REAL33D-AGENT-MVP-001.md`.
- No actual Ollama server/model was present. The HTTP adapter has a mocked
  response test only: `IMPLEMENTED_UNVERIFIED` for real inference.

## PASS, limits, risks and next work

`REAL33D-AGENT-MVP-001 = PASS` for one autonomous 2D mock player under normal
Fusion32 rules. The user instructed a stop after that proof; no further agent
expansion is required in this milestone. Separate live `follow` and sustained
low-HP survival are `IMPLEMENTED_UNVERIFIED`. The learned 3577 candidate is
based on A's client-visible prior loot; in a fresh fixture the generic
loot-then-use path is available, but one-session eat after fresh loot has a
policy test and was not independently live observed. Normal human 2D play is
untouched unless `R33D_AGENT=1` is set.

If the operator starts a later milestone, first read this handoff and
`evidence/agent/REAL33D-AGENT-MVP-001.md`, inspect
`agent/real33d2d/modules/real33d_agent/agent_core.lua::mockBrain` and
`agent_runtime.lua::observe/dispatch`, run the LuaJIT test above, then use the
PowerShell launcher against a prepared QA runtime. Keep the ordinary player
rights check and visible-state boundary. Do not infer real LLM or low-HP PASS
from the mock run.
