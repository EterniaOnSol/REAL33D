# Curated Login Reference

Source: `login.bundle` from `tibiacacaca.zip`.

Revision: `f1c839fe7c0334fa036549a641487f21205d0129`; tree `400ec36b97a4f6aaf8380527b012fb3ab882a2e1`.

All 11 upstream files present here were extracted byte-for-byte with `core.autocrlf=false`. Upstream tracked `tibia.pem` is deliberately omitted because it is compromised private-key material. `config.cfg.dist` is retained as source evidence, but its sample credential must be replaced in generated, untracked runtime configuration. This documentation file is local metadata and is not part of the upstream tree.
