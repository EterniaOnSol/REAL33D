; No action or axis mappings, and no Enhanced Input mapping context.
;
; Walk keys are bound in C++ with BindKey in Real33DPlayerController, so the
; project needs no binary input asset and the whole input path is readable in
; the diff. See that file for why input produces an intent and never a
; movement.

[/Script/Engine.InputSettings]
bAltEnterTogglesFullscreen=True
bCaptureMouseOnLaunch=False
DefaultViewportMouseCaptureMode=NoCapture
DefaultViewportMouseLockMode=DoNotLock
