# Rejected

Declined proposals and superseded versions. Nothing here is deleted.

```text
visual/rejected/obj103/v1/
```

A rejected direction is often revived for a different asset or region, and a
decision record without the thing it rejected is not a record. Keep the
director's reason in the version's `NOTE.md` when there is one.
