# Approved

The versions the project director accepted, copied here from `mockups/` rather
than moved, so the proposal history stays intact.

```text
visual/approved/obj103/v2/
```

Record the version in the tracker's `Approved Version` column. An asset only
proceeds to `MODELING` once it appears here.
